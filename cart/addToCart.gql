mutation addToCart(
  $qty: Int!
  $pid: ID!
  $vid: ID
  $options: String
  $vendor: ID
  $replace: Boolean
  $store: String
) {
  addToCart(
    qty: $qty
    pid: $pid
    vid: $vid
    options: $options
    vendor: $vendor
    replace: $replace
    store: $store
  ) {
    items {
      pid
      vid
      barcode
      name
      slug
      qty
      price
      shippingCharge
      tax
      img
      options
      brandName
      vendorFirstName
      vendorLastName
      vendorPhone
    }
    qty
    subtotal
    discount {
      code
      value
      text
      amount
    }
    shipping {
      charge
    }
    tax
    total
    store {
      id
      name
      email
    }
  }
}

# for multiple vendor in futute
# {
#   "pid": "60151b263101e96f152d5165",
#   "qty": 1
# }
