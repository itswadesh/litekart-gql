mutation createEmailTemplateInStore($storeId: ID) {
  createEmailTemplateInStore(storeId: $storeId)
}

# storeId not mandatory(in case of not passing , automaticaly take the ccurrent logged in user store)
