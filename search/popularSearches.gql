query popularSearches(
  $page: Int
  $limit: Int
  $search: String
  $sort: String
  $store: ID
) {
  popularSearches(
    page: $page
    search: $search
    limit: $limit
    sort: $sort
    store: $store
  ) {
    count
    page
    pageSize
    data {
      id
      text
      popularity
      store {
        id
        name
        email
        phone
      }
    }
  }
}
