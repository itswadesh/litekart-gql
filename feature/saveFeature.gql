mutation saveFeature(
  $id: String!
  $name: String
  $value: String
  $product: ID
  $active: Boolean
) {
  saveFeature(
    id: $id
    name: $name
    value: $value
    product: $product
    active: $active
  ) {
    id
    name
    value
    slug
    product
    active
  }
}
