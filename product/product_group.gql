query product_group($id: ID!) {
  product_group(id: $id) {
    sizeGroup {
      id
      slug
      size {
        id
        name
      }
    }
    colorGroup {
      id
      slug
      color {
        id
        name
        color_code
      }
    }
  }
}
