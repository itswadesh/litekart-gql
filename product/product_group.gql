query product_group($id: ID!) {
  product_group(id: $id) {
    size_group {
      id
      slug
      size {
        id
        name
      }
    }
    color_group {
      id
      slug
      color {
        id
        name
        color_code
      }
    }
  }
}
