mutation saveColor(
  $id: String!
  $name: String
  $color_code: String
  $info: String
  $featured: Boolean
  $slug: String
  $active: Boolean
) {
  saveColor(
    id: $id
    name: $name
    color_code: $color_code
    info: $info
    slug: $slug
    featured: $featured
    active: $active
  ) {
    id
    name
    color_code
    info
    slug
    featured
    active
    createdAt
    updatedAt
  }
}
